[package]
name = "chatwca-network-helper"
version = "0.0.1-phase0"
edition = "2021"
publish = false

[dependencies]
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"
