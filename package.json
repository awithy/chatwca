{
  "name": "chatwca",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "clean": "rm -rf dist coverage",
    "predev": "npm run build:sandbox-worker",
    "dev": "concurrently --kill-others-on-fail --names server,web \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "tsx watch src/server/index.ts",
    "dev:web": "vite",
    "build": "npm run clean && npm run build:sandbox-worker && npm run build:server && npm run build:web",
    "build:sandbox-worker": "node scripts/build-sandbox-worker.mjs",
    "build:server": "tsc -p tsconfig.server.json",
    "build:web": "vite build",
    "start": "node dist/server/server/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.server.json && tsc --noEmit -p tsconfig.web.json && tsc --noEmit -p tsconfig.tests.json",
    "test": "vitest run --passWithNoTests",
    "test:unit": "vitest run tests/unit --passWithNoTests",
    "pretest:integration": "npm run build:sandbox-worker",
    "test:integration": "vitest run tests/integration --passWithNoTests",
    "pretest:sandbox-real": "npm run build:sandbox-worker",
    "test:sandbox-real": "CHATWCA_SANDBOX_CAPABLE=1 vitest run tests/integration/sandbox-probe.test.ts tests/integration/sandbox-worker-client.test.ts tests/integration/sandbox-pi-runtime.test.ts tests/integration/sandbox-attack-concurrency.test.ts --passWithNoTests",
    "test:browser": "playwright test --pass-with-no-tests",
    "test:sdk-smoke": "node --test tests/smoke/pi-sdk.test.mjs",
    "spike:sandbox-profile": "node scripts/spike-bubblewrap-profile.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@sinclair/typebox": "^0.34.48",
    "better-sqlite3": "^13.0.3",
    "express": "^5.2.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.84.3",
    "@playwright/test": "^1.58.2",
    "@types/better-sqlite3": "^9.6.0",
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.5",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@types/ws": "^8.18.1",
    "@vitejs/plugin-react": "^5.1.4",
    "concurrently": "^9.2.1",
    "esbuild": "^0.28.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
